#macro iptr "i64"

use CmdPrint
use MemAlloc
use MemReAlloc
use MemFree

struct MyStruct{
	i8 myByte ?
	i64 myLong ?
}

i8 msg 'myInt is %d', 10, 0
i64 myInt 120
MyStruct myStructTest {8, 8}

func main(iptr args){
	i64 myLongVar = 0
	MyStruct myStructVar

	CmdPrint(&msg,myInt)
	print_i64(myInt)
	print_i64(myStructTest.mylong)
	return 0
}

func print_i64(i64 integer){
	CmdPrint(&msg,integer)
	return
}